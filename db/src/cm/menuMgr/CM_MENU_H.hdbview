VIEW CM_MENU_H AS
SELECT * FROM HIERARCHY (
  SOURCE (
    SELECT MENU_CODE AS NODE_ID
        ,  PARENT_MENU_CODE AS PARENT_ID
        ,  M.*
      FROM CM_MENU M
  ) 
  START WHERE PARENT_MENU_CODE IS NULL
  SIBLING ORDER BY PARENT_ID 
)
ORDER BY HIERARCHY_RANK
;