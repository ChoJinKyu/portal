VIEW SP_SC_TEST_SESSION_CONTEXT (
  "HOST",
  "PORT",
  "CONNECTION_ID",
  "KEY",
  "VALUE",
  "SECTION"
) AS
SELECT "HOST",
  "PORT",
  "CONNECTION_ID",
  "KEY",
  "VALUE",
  "SECTION"
FROM SP_SC_SYSM_SESSION_CONTEXT
WHERE CONNECTION_ID = CURRENT_CONNECTION;