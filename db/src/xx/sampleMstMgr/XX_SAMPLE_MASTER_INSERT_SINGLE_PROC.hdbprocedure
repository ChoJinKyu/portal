PROCEDURE XX_SAMPLE_MASTER_INSERT_SINGLE_PROC (
    in MASTER_ID BIGINT,
    in CD NVARCHAR(5000),
    in NAME NVARCHAR(5000)
)

LANGUAGE SQLSCRIPT
SQL SECURITY INVOKER 

AS
BEGIN
    INSERT INTO XX_SAMPLE_MASTER
    VALUES(
        :MASTER_ID,
        :CD,
        :NAME
    );

END