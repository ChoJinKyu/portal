VIEW XX_CATEGORIES_H AS
SELECT * FROM HIERARCHY (
  SOURCE (
    SELECT ID AS NODE_ID
        , PARENT_ID
        , NAME
        , DESCR
      FROM XX_CATEGORIES
  ) 
  START WHERE PARENT_ID IS NULL
  SIBLING ORDER BY PARENT_ID 
)
ORDER BY HIERARCHY_RANK
;