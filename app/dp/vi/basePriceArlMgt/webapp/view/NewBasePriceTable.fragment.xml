<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:form="sap.ui.layout.form"
    xmlns:t="sap.ui.table"
    xmlns:layout="sap.ui.layout" >
    <t:Table 
        selectionMode="MultiToggle"
        rows="{detailModel>/details}"
        rowSelectionChange=".onRowSelectionChange"
        fixedColumnCount="3"
        visibleRowCount="5">
        <t:extension>
            <OverflowToolbar class="sapFDynamicPageAlignContent">
                <ToolbarSpacer />
                <Button text="{I18N>/UPLOAD}" type="Transparent" press=".onDeveloping" visible="{detailViewModel>/viewMode}" />
                <Button text="{I18N>/ADDITION}" type="Transparent" press=".onAddBasePrice" visible="{detailViewModel>/viewMode}" />
                <Button text="{I18N>/DELETE}" type="Transparent" press=".onDeleteDetailRow" visible="{detailViewModel>/viewMode}" />
                <!-- <Button icon="sap-icon://excel-attachment" />
                <Button icon="sap-icon://group-2" />
                <Button icon="sap-icon://action-settings" /> -->
            </OverflowToolbar>
        </t:extension>
        <t:columns>
            <!-- <t:Column hAlign="Center" width="6rem"> -->
            <t:Column width="18rem">
                <Label text="{I18N>/COMPANY}" textAlign="Center" width="100%" />
                <t:template>
                    <!-- <ComboBox width="100%" 
                        editable="{= ${detailViewModel>/viewMode} &amp;&amp; ${rootModel>/config/DP_VI_COMPANY_EDITABLE_FLAG} === 'Y' ? true : false}"
                        selectedKey="{detailModel>company_code}" 
                        items="{
                            path: 'org>/Company',
                            filters: [
                                {path: 'tenant_id', operator: 'EQ', value1: 'L2100'}
                            ],
                            sorter: { path: 'company_code'},
                            templateShareable:false
                        }"
                        change=".onChangeCompany">
                        <items>
                            <core:Item key="{org>company_code}" text="[{org>company_code}] {org>company_name}" />
                        </items>
                    </ComboBox> -->
                    <Text text="{detailModel>company_code}" />
                </t:template>
            </t:Column>
            <!-- <t:Column hAlign="Center" width="6rem"> -->
            <t:Column width="18rem">
                <Label  text="{I18N>/VI_APPROVE_ORG_CODE}" textAlign="Center" width="100%" />
                <t:template>
                    <!-- <Text text="{detailModel>org_code}" /> -->
                    <!-- <ComboBox width="100%"
                        editable="{detailViewModel>/viewMode}"
                        selectedKey="{
                            path: 'detailModel>org_code',
                            templateShareable:false}" 
                        items="{
                            path: 'detailModel>purOrg',
                            templateShareable:false}">
                        <items>
                            <core:Item key="{detailModel>org_code}" text="[{detailModel>org_code}]{detailModel>org_name}" />
                        </items>
                    </ComboBox> -->
                    <Text text="{detailModel>org_name}" />
                </t:template>
            </t:Column>
            <t:Column width="10rem">
                <Label text="{I18N>/MATERIAL_CODE}" textAlign="Center" width="100%" />
                <t:template>
                    <!-- <Input
                        value="{detailModel>material_code}"
                        editable="{= ${detailModel>/approve_status_code} === '10' ? true : false}"
                        showValueHelp="true"
                        valueHelpRequest=".onGetMaterialCodeDialogData" >
                        <customData>
                            <core:CustomData key="dialog" value="materialCode"  />
                        </customData>
                    </Input> -->
                    <VBox>
                    <Input
                        visible="{detailViewModel>/viewMode}"
                        value="{detailModel>material_code}"
                        showValueHelp="true"
                        valueHelpRequest=".onMaterialMasterMultiDialogPress($event, 'New')">
                    </Input>
                    <Text text="{detailModel>material_code}" visible="{= !${detailViewModel>/viewMode}}" />
                    </VBox>
                </t:template>
            </t:Column>
            <t:Column width="14rem">
                <Label text="{I18N>/DESCRIPTION}" textAlign="Center" width="100%" />
                <t:template>
                    <Text text="{detailModel>material_desc}" />
                </t:template>
            </t:Column>
            <t:Column width="20rem">
                <Label text="{I18N>/SPEC_DESC}" textAlign="Center" width="100%" />
                <t:template>
                    <Text text="{detailModel>material_spec}" />
                </t:template>
            </t:Column>
            <t:Column width="8rem">
                <Label text="{I18N>/BASE_UOM_CODE}" textAlign="Center" width="100%" />
                <t:template>
                    <Text text="{detailModel>base_uom_code}" textAlign="Center" width="100%" />
                </t:template>
            </t:Column>
            <t:Column width="14rem">
                <Label text="{I18N>/BASIS}" textAlign="Center" width="100%" />
                <t:template>
                    <VBox>
                    <Select
                        width="100%"
                        visible="{detailViewModel>/viewMode}"
                        items="{
                            path: 'detailViewModel>/basis',
                            templateShareable:false}"
                        selectedKey="{detailModel>base_price_ground_code}"
                        change=".onChangeBasis">
                        <items>
                            <core:Item key="{detailViewModel>code}" text="{detailViewModel>code_name}" />
                        </items>
                    </Select>
                    <Text text="{detailModel>base_price_ground_code_nm}" visible="{= !${detailViewModel>/viewMode}}" />
                    </VBox>
                </t:template>
            </t:Column>
            <t:Column headerSpan="2" width="7rem" visible="{= ${rootModel>/config/DP_VI_SUPPLY_DISPLAY_FLAG} === 'Y' ? true : false}">
                <t:multiLabels>
                    <Label text="{I18N>/SUPPLIER}" textAlign="Center" width="100%" />
                    <Label text="{I18N>/CODE}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <Text text="{detailModel>supplier_code}" textAlign="Center" width="100%" />
                </t:template>
            </t:Column>
            <t:Column width="15rem" visible="{= ${rootModel>/config/DP_VI_SUPPLY_DISPLAY_FLAG} === 'Y' ? true : false}">
                <t:multiLabels>
                    <Label text="{I18N>/SUPPLIER}" textAlign="Center" width="100%" />
                    <Label text="{I18N>/NAME}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <!-- <Input
                        value="{detailModel>supplier_local_name}"
                        editable="{detailViewModel>/viewMode}"
                        showValueHelp="true"
                        valueHelpRequest=".onGetSupplierDialogData" >
                        <customData>
                            <core:CustomData key="dialog" value="supplier"  />
                        </customData>
                    </Input> -->
                    <VBox>
                    <Input
                        visible="{detailViewModel>/viewMode}"
                        value="{detailModel>supplier_local_name}"
                        tooltip="{detailModel>supplier_local_name}"
                        showValueHelp="true"
                        valueHelpRequest=".onSupplierDialogPress">
                    </Input>
                    <Text text="{detailModel>supplier_local_name}" visible="{= !${detailViewModel>/viewMode}}" />
                    </VBox>
                </t:template>
            </t:Column>

            <!--market_code 0 시작 -->
            <t:Column width="10rem" visible="{= ${rootModel>/config/DP_VI_MARKETCODE0_DISPLAY_FLAG} === 'Y' ? true : false}">
                <t:multiLabels>
                <Label text="{I18N>/BASE_DATE}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <Text text="{
                        path: 'detailModel>base_date',
                        formatter: '.dateFormatter.toDateString'}" textAlign="Center" width="100%" />
                </t:template>
            </t:Column>
            <t:Column width="10rem" visible="{= ${rootModel>/config/DP_VI_MARKETCODE0_DISPLAY_FLAG} === 'Y' ? true : false}">
                <Label text="{I18N>/NEW_BASE_PRICE}" textAlign="Center" width="100%" />
                <t:template>
                    <VBox>
                    <Input 
                        value="{
                                parts: [
                                    {path: 'detailModel>prices/0/new_base_price'},
                                    {path: 'detailModel>prices/0/new_base_price_currency_code'}
                                ],
                                type: 'sap.ui.model.type.Currency',
                                formatOptions: {
                                    showMeasure: false
                                }
                            }" 
                        visible="{= ${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}  ? true : false }"
                        editable="{= ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode} ? true : false }"
                        textAlign="End" />
                    <Text text="{
                                parts: [
                                    {path: 'detailModel>prices/0/new_base_price'},
                                    {path: 'detailModel>prices/0/new_base_price_currency_code'}
                                ],
                                type: 'sap.ui.model.type.Currency',
                                formatOptions: {
                                    showMeasure: false
                                }
                            }" 
                        visible="{= !(${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}) ? true : false }"
                        textAlign="End" width="100%" />
                    </VBox>
                </t:template>
            </t:Column>
            <t:Column width="6rem" visible="{= ${rootModel>/config/DP_VI_MARKETCODE0_DISPLAY_FLAG} === 'Y' ? true : false}">
                <Label text="{I18N>/CURRENCY_CODE}" textAlign="Center" width="100%" />
                <t:template>
                    <VBox>
                    <ComboBox
                        visible="{= ${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}  ? true : false }"
                        editable="{= ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode} ? true : false }"
                        items="{
                            path: 'detailViewModel>/currency',
                            templateShareable:false}"
                        selectedKey="{detailModel>prices/0/new_base_price_currency_code}">
                        <items>
                            <core:Item key="{detailViewModel>currency_code}" text="{detailViewModel>currency_code}" />
                        </items>
                    </ComboBox>
                    <Text text="{detailModel>prices/0/new_base_price_currency_code}" visible="{= !(${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}) ? true : false }" textAlign="Center" width="100%" />
                    </VBox>
                </t:template>
            </t:Column>
            <!--market_code 0 끝 -->

            <!--DOMESTIC(market_code 2) 시작 -->
            <t:Column headerSpan="3" width="10rem" visible="{= ${rootModel>/config/DP_VI_MARKETCODE2_DISPLAY_FLAG} === 'Y' ? true : false}">
                <t:multiLabels>
                    <Label text="{I18N>/DOMESTIC}" textAlign="Center" width="100%" />
                    <Label text="{I18N>/BASE_DATE}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <Text text="{
                        path: 'detailModel>base_date',
                        formatter: '.dateFormatter.toDateString'}" textAlign="Center" width="100%" />
                </t:template>
            </t:Column>
            <t:Column width="10rem" visible="{= ${rootModel>/config/DP_VI_MARKETCODE2_DISPLAY_FLAG} === 'Y' ? true : false}">
                <t:multiLabels>
                    <Label text="{I18N>/DOMESTIC}" textAlign="Center" width="100%" />
                    <Label text="{I18N>/NEW_BASE_PRICE}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <VBox>
                    <Input 
                        value="{
                                parts: [
                                    {path: 'detailModel>prices/2/new_base_price'},
                                    {path: 'detailModel>prices/2/new_base_price_currency_code'}
                                ],
                                type: 'sap.ui.model.type.Currency',
                                formatOptions: {
                                    showMeasure: false
                                }
                            }" 
                        visible="{= ${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}  ? true : false }"
                        enabled="{= ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode} ? true : false }"
                        textAlign="End" />
                    <Text text="{
                                parts: [
                                    {path: 'detailModel>prices/2/new_base_price'},
                                    {path: 'detailModel>prices/2/new_base_price_currency_code'}
                                ],
                                type: 'sap.ui.model.type.Currency',
                                formatOptions: {
                                    showMeasure: false
                                }
                            }" 
                        visible="{= !(${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}) ? true : false }" textAlign="End" width="100%" />
                    </VBox>
                </t:template>
            </t:Column>
            <t:Column width="6rem" visible="{= ${rootModel>/config/DP_VI_MARKETCODE2_DISPLAY_FLAG} === 'Y' ? true : false}">
                <t:multiLabels>
                    <Label text="{I18N>/DOMESTIC}" textAlign="Center" width="100%" />
                    <Label text="{I18N>/CURRENCY_CODE}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <VBox>
                    <ComboBox
                        visible="{= ${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}  ? true : false }"
                        enabled="{= ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode} ? true : false }"
                        items="{
                            path: 'detailViewModel>/currency',
                            templateShareable:false}"
                        selectedKey="{detailModel>prices/2/new_base_price_currency_code}">
                        <items>
                            <core:Item key="{detailViewModel>currency_code}" text="{detailViewModel>currency_code}" />
                        </items>
                    </ComboBox>
                    <Text text="{detailModel>prices/2/new_base_price_currency_code}" visible="{= !(${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}) ? true : false }" textAlign="Center" width="100%" />
                    </VBox>
                </t:template>
            </t:Column>
            <!--DOMESTIC(market_code 2) 끝 -->

            <!--EXPORT(market_code 1) 시작 -->
            <t:Column headerSpan="3" width="10rem" visible="{= ${rootModel>/config/DP_VI_MARKETCODE1_DISPLAY_FLAG} === 'Y' ? true : false}">
                <t:multiLabels>
                    <Label text="{I18N>/EXPORT}" textAlign="Center" width="100%" />
                    <Label text="{I18N>/BASE_DATE}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <Text text="{
                        path: 'detailModel>base_date',
                        formatter: '.dateFormatter.toDateString'}" textAlign="Center" width="100%" />
                </t:template>
                </t:Column>
            <t:Column width="10rem" visible="{= ${rootModel>/config/DP_VI_MARKETCODE1_DISPLAY_FLAG} === 'Y' ? true : false}">
                <t:multiLabels>
                    <Label text="{I18N>/EXPORT}" textAlign="Center" width="100%" />
                    <Label text="{I18N>/NEW_BASE_PRICE}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <VBox>
                    <Input 
                        value="{
                                parts: [
                                    {path: 'detailModel>prices/1/new_base_price'},
                                    {path: 'detailModel>prices/1/new_base_price_currency_code'}
                                ],
                                type: 'sap.ui.model.type.Currency',
                                formatOptions: {
                                    showMeasure: false
                                }
                            }" 
                        visible="{= ${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}  ? true : false }"
                        enabled="{= ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode} ? true : false }"
                        textAlign="End" />
                    <Text text="{
                                parts: [
                                    {path: 'detailModel>prices/1/new_base_price'},
                                    {path: 'detailModel>prices/1/new_base_price_currency_code'}
                                ],
                                type: 'sap.ui.model.type.Currency',
                                formatOptions: {
                                    showMeasure: false
                                }
                            }" 
                        visible="{= !(${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}) ? true : false }" textAlign="End" width="100%" />
                    </VBox>
                </t:template>
                </t:Column>
            <t:Column width="6rem" visible="{= ${rootModel>/config/DP_VI_MARKETCODE1_DISPLAY_FLAG} === 'Y' ? true : false}">
                <t:multiLabels>
                    <Label text="{I18N>/EXPORT}" textAlign="Center" width="100%" />
                    <Label text="{I18N>/CURRENCY_CODE}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <VBox>
                    <ComboBox
                        visible="{= ${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}  ? true : false }"
                        items="{
                            path: 'detailViewModel>/currency',
                            templateShareable:false}"
                            enabled="{= ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode} ? true : false }"
                        selectedKey="{detailModel>prices/1/new_base_price_currency_code}">
                        <items>
                            <core:Item key="{detailViewModel>currency_code}" text="{detailViewModel>currency_code}" />
                        </items>
                    </ComboBox>
                    <Text text="{detailModel>prices/1/new_base_price_currency_code}" visible="{= !(${detailViewModel>/viewMode} &amp;&amp; ${detailModel>base_price_ground_code} !== ${detailViewModel>/familyMaterialCodeShowCode}) ? true : false }" textAlign="Center" width="100%" />
                    </VBox>
                </t:template>
            </t:Column>
            <!--EXPORT(market_code 1) 끝 -->

            <t:Column width="12rem">
                <Label text="{I18N>/BASE_PRICE_TYPE}" textAlign="Center" width="100%" />
                <t:template>
                    <Text text="{detailModel>base_price_type}" />
                </t:template>
            </t:Column>
            <!-- <t:Column headerSpan="4" width="6rem">
                <t:multiLabels>
                    <Label text="{I18N>/FAMILY_MATERIAL_CODE}" textAlign="Center" width="100%" />
                    <Label  text="{I18N>/VI_APPROVE_ORG_CODE}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <Text text="{detailModel>PART_NO}" />
                </t:template>
            </t:Column> -->
            <t:Column headerSpan="3" width="12rem">
                <t:multiLabels>
                    <Label text="{I18N>/FAMILY_MATERIAL_CODE}" textAlign="Center" width="100%" />
                    <Label text="{I18N>/MATERIAL_CODE}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <Input
                        value="{detailModel>family_material_code}"
                        editable="{detailViewModel>/viewMode}"
                        visible="{= ${detailModel>base_price_ground_code} === ${detailViewModel>/familyMaterialCodeShowCode} &amp;&amp; ${detailViewModel>/viewMode} ? true : false }"
                        showValueHelp="true"
                        valueHelpRequest=".onOpenFamilyMaterialCodeDialog" >
                    </Input>
                </t:template>
                </t:Column>
            <t:Column width="12rem">
                <t:multiLabels>
                    <Label text="{I18N>/FAMILY_MATERIAL_CODE}" textAlign="Center" width="100%" />
                    <Label text="{I18N>/SUPPLIER_CODE}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <Text text="{detailModel>family_supplier_code}" />
                </t:template>
            </t:Column>
            <t:Column width="12rem">
                <t:multiLabels>
                    <Label text="{I18N>/FAMILY_MATERIAL_CODE}" textAlign="Center" width="100%" />
                    <Label text="{I18N>/SUPPLIER_NAME}" textAlign="Center" width="100%" />
                </t:multiLabels>
                <t:template>
                    <Text text="{detailModel>family_supplier_name}" />
                </t:template>
            </t:Column>
            <!-- <t:Column width="12rem">
                <Label text="{I18N>/HQ_NPD_BASE_PRICE}" textAlign="Center" width="100%" />
                <Label text="HQ NPD Base Price" textAlign="Center" width="100%" />
                <t:template>
                    <Text text="{detailModel>hq_npd_base_price}" textAlign="Right" width="100%" />
                </t:template>
            </t:Column> -->
        </t:columns>
    </t:Table>
</core:FragmentDefinition>