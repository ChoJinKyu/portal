<mvc:View controllerName="dp.vi.basePriceArlMgt.controller.BasePriceDetail"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:uxap="sap.uxap"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:form="sap.ui.layout.form"
    xmlns:layout="sap.ui.layout"
    xmlns:core="sap.ui.core"
    xmlns:lib-m="ext.lib.m"
    xmlns:t="sap.ui.table"
    xmlns:co="sap.suite.ui.commons"
    xmlns:rte="sap.ui.richtexteditor"
    height="100%">

    <uxap:ObjectPageLayout
        showTitleInHeaderContent="true"
		showEditHeaderButton="true"
        upperCaseAnchorBar="false"
        headerContentPinnable="false"
        showAnchorBarPopover="false"
        showFooter="true">

		<uxap:headerTitle>
			<uxap:ObjectPageDynamicHeaderTitle>
				<uxap:expandedHeading>
                    <HBox alignItems="End">
                        <OverflowToolbarButton type="Transparent" icon="sap-icon://nav-back" press=".onBack" />
                        <Title text="{I18N>/NEW_BASE_PRICE}" wrapping="true"/>
                    </HBox>
				</uxap:expandedHeading>
                <!-- <uxap:navigationActions>
					<OverflowToolbarButton icon="sap-icon://decline" text="{I18N>/BACK}" press=".onBack" />
                </uxap:navigationActions> -->
			</uxap:ObjectPageDynamicHeaderTitle>
		</uxap:headerTitle>

		<uxap:headerContent>
			<FlexBox wrap="Wrap">
                <!-- Requester 시작 -->
                <layout:VerticalLayout class="sapUiLargeMarginBeginEnd">
                    <ObjectStatus title="{I18N>/APPROVAL_NUMBER}" text="{detailModel>/approval_number}"/>
                    <ObjectStatus title="{I18N>/REQUESTOR}" text="{detailModel>/requestor_local_nm}"/>
                    <ObjectStatus title="{I18N>/REQUEST_DATE}" text="{path:'detailModel>/local_update_dtm', formatter: '.dateFormatter.toDateString'}"/>
                </layout:VerticalLayout>
                <!-- Requester 끝 -->

                <!-- Status 시작 -->
				<layout:VerticalLayout class="sapUiLargeMarginBeginEnd">
					<Label text="{I18N>/STATUS} :"/>
                    <ObjectStatus class="sapMObjectStatusLarge" text="{path:'detailModel>/approve_status_code', formatter: '.onSetStatus'}" state="Error" />
				</layout:VerticalLayout>
                <!-- Status 끝 -->

                <!-- Process 시작 -->
                <layout:VerticalLayout class="sapUiLargeMarginBeginEnd">
					<Label text="{I18N>/PROCESS} : "/>
                    <HBox>
                        <co:MicroProcessFlow>
                            <co:content>
                                <co:MicroProcessFlowItem 
                                    state="{= ${detailModel>/approve_status_code} === 'DR' ? 'Error' : 'None'}" 
                                    stepWidth="5rem" 
                                    icon="{= ${detailModel>/approve_status_code} === 'DR' ? 'sap-icon://circle-task' : 'sap-icon://complete'}" />
                                <co:MicroProcessFlowItem 
                                    state="{= ${detailModel>/approve_status_code} === 'AR' ? 'Error' : 'None'}" 
                                    stepWidth="5rem"
                                    icon="{= ${detailModel>/approve_status_code} === 'AR' ? 'sap-icon://circle-task' : (${detailModel>/approve_status_code} === 'DR' ? '' : 'sap-icon://complete')}" />
                                <co:MicroProcessFlowItem 
                                    state="{= ${detailModel>/approve_status_code} === 'AP' ? 'Error' : 'None'}" 
                                    stepWidth="5rem"
                                    icon="{= ${detailModel>/approve_status_code} === 'AP' ? 'sap-icon://circle-task' : ''}" />
                            </co:content>
                        </co:MicroProcessFlow>
                    </HBox>
                    <HBox>
                        <Text text="{rootModel>/processList/0/code_name}" width="4rem" />
                        <Text text="{rootModel>/processList/1/code_name}" width="6rem" textAlign="Center" />
                        <Text text="{rootModel>/processList/2/code_name}" width="6rem" textAlign="Center" />
                    </HBox>
				</layout:VerticalLayout>
                <!-- Process 시작 -->
			</FlexBox>
		</uxap:headerContent>

        <uxap:sections>
            <!-- General Information 시작 -->
			<uxap:ObjectPageSection titleUppercase="false" title="{I18N>/GENERAL_INFO}">
				<uxap:subSections>
					<uxap:ObjectPageSubSection>
						<uxap:blocks>
                            <VBox>
                                <HBox alignItems="Center">
                                    <Label
                                        text="{I18N>/TITLE}"
                                        class="sapUiTinyMarginEnd"
                                        textAlign="Left"
                                        labelFor="input"
                                        required="true"/>
                                    <Input
                                        width="20rem"
                                        placeholder=""
                                        editable="{detailViewModel>/viewMode}"
                                        value="{detailModel>/approval_title}">
                                    </Input>
                                </HBox>
                                <VBox>
                                    <Label text="{I18N>/DESCRIPTION}" required="true"/>
                                    <!-- Text Editor 시작 -->
                                    <!-- <layout:VerticalLayout id="idVerticalLayout" class="sapUiContentPadding" width="100%">
                                    </layout:VerticalLayout> -->
                                    <rte:RichTextEditor 
                                        width="100%" 
                                        height="200px" 
                                        value="{detailModel>/approval_contents}" 
                                        customToolbar="true" 
                                        showGroupFont="true" 
                                        showGroupLink="true" 
                                        showGroupInsert="true"
                                        editable="{detailViewModel>/viewMode}" />
                                    <!-- Text Editor 끝 -->
                                </VBox>
                            </VBox>
						</uxap:blocks>
					</uxap:ObjectPageSubSection>
                </uxap:subSections>
            </uxap:ObjectPageSection>
            <!-- General Information 끝 -->

            <!-- Material Code 시작 -->
            <uxap:ObjectPageSection titleUppercase="false" title="{I18N>/BASE_PRICE_LIST}({detailViewModel>/detailsLength})">
				<uxap:subSections>
					<uxap:ObjectPageSubSection id="bacePriceTableSection">
					</uxap:ObjectPageSubSection>
                </uxap:subSections>
            </uxap:ObjectPageSection>
            <!-- Material Code 끝 -->

            <!-- Attachment 시작 -->
			<uxap:ObjectPageSection titleUppercase="false" title="{I18N>/ATTACHMENT}">
				<uxap:subSections>
					<uxap:ObjectPageSubSection>
						<uxap:blocks>
                            <!-- File Upload 시작-->
                            <UploadCollection
                                id="UploadCollection"
                                maximumFilenameLength="55"
                                maximumFileSize="10"
                                multiple="true"
                                sameFilenameAllowed="true"
                                instantUpload="false"
                                noDataDescription="Drop files or use the &quot;Add&quot; button to keep files ready for upload"
                                change="onChange"
                                fileDeleted="onFileDeleted"
                                filenameLengthExceed="onFilenameLengthExceed"
                                fileSizeExceed="onFileSizeExceed"
                                typeMissmatch="onTypeMissmatch"
                                uploadComplete="onUploadComplete"
                                beforeUploadStarts="onBeforeUploadStarts" />
                            <!-- File Upload 끝-->
						</uxap:blocks>
					</uxap:ObjectPageSubSection>
                </uxap:subSections>
            </uxap:ObjectPageSection>
            <!-- GAttachment 끝 -->

            <!-- Report Line 시작 -->
			<uxap:ObjectPageSection titleUppercase="false" title="{I18N>/APPROVAL_LINE}">
				<uxap:subSections>
					<uxap:ObjectPageSubSection>
						<uxap:blocks>
                            <!-- Approval & Reference 시작 -->
                            <VBox>
                            <Table id="ApprovalTable"
                                    growing="true"
                                    growingThreshold="20"
                                    class="sapFDynamicPageAlignContent"
                                    mode="None"
                                    width="auto"
                                    items="{appdetailModel>/undefined}"
                                    >
                                <headerToolbar>
                                    <OverflowToolbar>
                                    <Label text="Approval Line"/>
                                    </OverflowToolbar>
                                </headerToolbar>
                                <columns>
                                    <Column minScreenWidth="Tablet" hAlign="Center" mergeDuplicates="true" width="3rem" >
                                        <Text text="No." />
                                    </Column>
                                    <Column minScreenWidth="Tablet" hAlign="Center" mergeDuplicates="true" width="6em" >
                                        <Text text="Type" />
                                    </Column>
                                    <Column minScreenWidth="Tablet" hAlign="Center" width="16em" >
                                        <Text text="Name/Dept" />
                                    </Column>
                                    <Column minScreenWidth="Tablet" hAlign="Center"  >
                                        <Text text="Comment" />
                                    </Column>
                                    <Column minScreenWidth="Tablet" hAlign="Center" width="6em" >
                                        <Text text="Status" />
                                    </Column>
                                    <Column minScreenWidth="Tablet" hAlign="Center" width="8em" >
                                        <Text text="Add / Delete" />
                                    </Column>
                                </columns>
                                <items>
                                    <ColumnListItem>
                                        <cells>
                                            <!-- <ObjectIdentifier title="{Name}" text="{ProductId}"/> -->
                                            <Text text="{appdetailModel>no}" />
                                            <ComboBox  width="100%"
                                                selectedKey="{appdetailModel>type}"
                                                enabled="{appdetailModel>editMode}"
                                                items="{
                                                    path: 'org>/Org_Company',
                                                    filters: [
                                                        {path: 'tenant_id', operator: 'EQ', value1: 'L1100'}
                                                    ],
                                                    sorter: { path: 'company_code'},
                                                    templateShareable:false
                                                }"
                                                >
                                            <core:Item key="{org>company_code}" text="[{org>company_code}] {org>company_name}" />
                                            </ComboBox>
                                            <SearchField enabled="{appdetailModel>editMode}" search=".onSearch" width="100%" placeholder="Search employee" value="{appdetailModel>nameDept}"/>
                                            <Text text="{appdetailModel>status}" />
                                            <Text text="{appdetailModel>comment}" />
                                            <Button icon="{appdetailModel>arrowUp}" press=".onSortUp(${appdetailModel>no})" tooltip="sort" visible="{= ${appdetailModel>arrowUp}===''?false:true }"></Button>
                                            <Button icon="{appdetailModel>arrowDown}" press=".onSortDown(${appdetailModel>no})"  visible="{= ${appdetailModel>arrowDown}===''?false:true }" tooltip="sort"></Button>
                                            <Button icon="sap-icon://delete" press=".setApprovalRemoveRow(${appdetailModel>no})" tooltip="삭제" visible="{appdetailModel>trashShow}"></Button>
                                        </cells>
                                    </ColumnListItem>
                                </items>
                            </Table>

                            <Text text="Referrer" />
                            <Input value="{detailModel>/abc}"
                                showValueHelp="true"
                                valueHelpRequest=".onOpenMaterialCode" />
                            </VBox>
                            <!-- Approval & Reference 끝 -->
						</uxap:blocks>
					</uxap:ObjectPageSubSection>
                </uxap:subSections>
            </uxap:ObjectPageSection>
            <!-- Report Line 끝 -->
        </uxap:sections>

        <!-- footer 시작 -->
        <uxap:footer>
			<OverflowToolbar visible="{= ${detailModel>/approve_status_code} === 'DR' ? true : false}">
				<ToolbarSpacer />
                <Button
                    text="{I18N>/CHANGE_DEVELOPER}"
                    enabled="{= ${detailViewModel>/viewMode}}"
                    press=".onDeveloping" />
                <Button
                    text="{I18N>/EDIT}" 
                    enabled="{= !${detailViewModel>/viewMode}}"
                    press=".onEditToggle" />
                <!-- <Button
                    text="{I18N>/PREVIEW}" 
                    enabled="{= ${detailModel>/approve_status_code} === '10' ? true : false}"
                    press=".onPreview" /> -->
                <!-- <Button
                    text="{I18N>/CANCEL}" press=".onBack" /> -->
				<Button
                    text="{I18N>/DRAFT}" 
                    enabled="{= ${detailViewModel>/viewMode}}"
                    press=".onDraft" />
                <Button
                    text="{I18N>/CANCEL}"
                    enabled="{= ${detailViewModel>/viewMode}}"
                    press=".onEditToggle" />
                <Button
                    text="{I18N>/DELETE}" 
                    enabled="{= ${detailViewModel>/viewMode} &amp;&amp; ${detailModel>/approval_number} !== '' ? true : false}"
                    press=".onDelete" />
                <Button
                    type="Emphasized"
                    text="{I18N>/REQUEST}"
                    enabled="{= ${detailViewModel>/viewMode} &amp;&amp; ${detailModel>/approval_number} !== '' ? true : false}"
                    press=".onRequest" />
			</OverflowToolbar>
		</uxap:footer>
        <!-- footer 끝 -->
    </uxap:ObjectPageLayout>
</mvc:View>
