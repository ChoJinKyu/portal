<mvc:View 
    xmlns="sap.m" 
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    controllerName="cm.organizationMgr.controller.MainList"
    >
	<Table
        id="UnitTable"
        growing="true"
        growingThreshold="30"
        items="{list>/Org_Unit}" 
        mode="MultiSelect"
        width="auto"
        >
        <headerToolbar>
            <OverflowToolbar>
                <content>
                    <!-- <Title text="{I18N>/TENANT}" level="H2"/>
                    <ComboBox   
                        id="search_tenant"
                        required="true"
                        showSecondaryValues= "true"
                        selectedKey="{view>/tenant_id}"
                        items="{
                            path : '/Org_Tenant'
                        }"
                    >
                        <core:ListItem 
                            key="{tenant_id}" 
                            text="{tenant_name}"
                            additionalText="{tenant_id}"/>
                    </ComboBox>
                    <SearchField id="midTableSearchField" width="10rem" search=".onPageSearchButtonPress" />
                    <Button id="midTableApplyFilterButton" text="조회" type="Transparent" press=".onPageSearchButtonPress" /> -->
                    <ToolbarSpacer/>
                    <Button id="mainTableAddButton" text="{I18N>/ROW_ADD}" type="Transparent" press=".onMainTableAddButtonPress" enabled="{= ${view>/add} === 'Unit' }"/>
                    <Button id="mainTableDeleteButton" text="{I18N>/ROW_DELETE}" type="Transparent" press=".onMainTableDeleteButtonPress" />
                    <Button id="mainTableSaveButton" text="{I18N>/SAVE}" type="Emphasized" press=".onMainTableSaveButtonPress" />
                </content>
            </OverflowToolbar>
        </headerToolbar>
        <columns>
            <Column id="mainColumnState" minScreenWidth="Tablet" hAlign="Center" width="40px" >
                <ObjectStatus icon="sap-icon://document" text="State" visible="false" />
            </Column>
            <Column id="tenant_id">
                <Text text="tenant_id"/>
            </Column>
            <Column id="bizunit_code">
                <Text text="bizunit_code"/>
            </Column>
            <Column id="bizunit_name">
                <Text text="bizunit_name"/>
            </Column>
            <Column id="use_flag" hAlign="Center" width="10%">
                <Text text="use_flag"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem>
                <cells>
                    <ObjectStatus icon="{
                                    path: 'list>_row_state_',
                                    formatter: '.formatter.toModelStateColumnIcon'
                                }" />
                </cells>
                <cells>
                    <Text text="{list>tenant_id}"/>
                </cells>
                <cells>
                    <Input value="{list>bizunit_code}" editable="{= ${list>_row_state_} === 'C' }"/>
                </cells>
                <cells>
                    <Input value="{list>bizunit_name}"/>
                </cells>
                <cells>
                    <Switch state="{list>use_flag}" customTextOn="Yes" customTextOff="No">
                        <layoutData>
                            <FlexItemData growFactor="1" />
                        </layoutData>
                    </Switch>
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</mvc:View>