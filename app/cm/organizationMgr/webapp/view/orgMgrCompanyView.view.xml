<mvc:View 
    xmlns="sap.m" 
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    controllerName="cm.organizationMgr.controller.MainList"
    >
	<Table
        id="CompanyTable"
        growing="true"
        growingThreshold="30"
        items="{list>/Org_Company}"
        mode="MultiSelect"
        width="auto"
        >
        <headerToolbar>
            <OverflowToolbar>
                <content>
                    <!-- <Title text="{I18N>/TENANT}" level="H2"/>
                    <ComboBox   
                        required="true"
                        id="search_tenant"
                        showSecondaryValues= "true"
                        selectedKey="{view>/tenant_id}"
                        items="{
                            path : '/Org_Tenant'
                        }"
                    >
                        <core:ListItem 
                            key="{tenant_id}" 
                            text="{tenant_name}"
                            additionalText="{tenant_id}"/>
                    </ComboBox>
                    <SearchField id="midTableSearchField" width="10rem" search=".onPageSearchButtonPress" />
                    <Button id="midTableApplyFilterButton" text="조회" type="Transparent" press=".onPageSearchButtonPress" /> -->
                    <ToolbarSpacer/>
                    <Button id="mainTableAddButton" text="{I18N>/ROW_ADD}" type="Transparent" press=".onMainTableAddButtonPress" enabled="{= ${view>/add} === 'Company' }"/>
                    <Button id="mainTableDeleteButton" text="{I18N>/ROW_DELETE}" type="Transparent" press=".onMainTableDeleteButtonPress" />
                    <Button id="mainTableSaveButton" text="{I18N>/SAVE}" type="Emphasized" press=".onMainTableSaveButtonPress" />
                </content>
            </OverflowToolbar>
        </headerToolbar>
        <columns>
            <Column id="mainColumnState" minScreenWidth="Tablet" hAlign="Center" width="40px" >
                <ObjectStatus icon="sap-icon://document" text="State" visible="false" />
            </Column>
            <Column id="tenant_id">
                <Text text="tenant_id"/>
            </Column>
            <Column id="company_code">
                <Text text="company_code"/>
            </Column>
            <Column id="company_name">
                <Text text="company_name"/>
            </Column>
            <Column id="use_flag" hAlign="Center" >
                <Text text="use_flag"/>
            </Column>
            <Column id="erp_type_code">
                <Text text="erp_type_code"/>
            </Column>
            <Column id="currency_code">
                <Text text="currency_code"/>
            </Column>
            <Column id="country_code">
                <Text text="country_code"/>
            </Column>
            <Column id="language_code">
                <Text text="language_code"/>
            </Column>
            <Column id="affiliate_code">
                <Text text="affiliate_code"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem>
                <cells>
                    <ObjectStatus icon="{
                                    path: 'list>_row_state_',
                                    formatter: '.formatter.toModelStateColumnIcon'
                                }" />
                </cells>
                <cells>
                    <Text text="{list>tenant_id}"/>
                </cells>
                <cells>
                    <Input value="{list>company_code}" editable="{= ${list>_row_state_} === 'C' }" required="true"/>
                </cells>
                <cells>
                    <Input value="{list>company_name}" required="true"/>
                </cells>
                <cells>
                    <!-- <Input value="{use_flag}"/> -->
                    <Switch state="{list>use_flag}" customTextOn="Yes" customTextOff="No">
                        <layoutData>
                            <FlexItemData growFactor="1" />
                        </layoutData>
                    </Switch>
                </cells>
                <cells>
                    <Input value="{list>erp_type_code}"/>
                </cells>
                <cells>
                    <Input value="{list>currency_code}"/>
                </cells>
                <cells>
                    <!-- <ComboBox
                    width="100%"
                    items="{
                        path: 'country>/Country',
                        filters: [
                        ]
                    }"
                    selectedKey="{list>country_code}">
                    <items>
                        <core:Item key="{country>country_code}" text="{country>country_code} : {country>iso_code}" value="{country>country_code}" valueLiveUpdate="true" />
                    </items>
                    </ComboBox> -->
                    <Input value="{list>country_code}"/>
                </cells>
                <cells>
                    <Input value="{list>language_code}"/>
                </cells>
                <cells>
                    <Input value="{list>affiliate_code}"/>
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</mvc:View>