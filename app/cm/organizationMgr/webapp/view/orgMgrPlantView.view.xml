<mvc:View 
    xmlns="sap.m" 
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    controllerName="cm.organizationMgr.controller.MainList"
    >
	<Table
        id="PlantTable"
        growing="true"
        growingThreshold="30"
        items="{list>/Org_Plant}" 
        mode="MultiSelect"
        width="auto"
        >
        <headerToolbar>
            <OverflowToolbar>
                <content>
                    <Title text="{I18N>/TENANT}" level="H2"/>
                    <ComboBox   
                        id="search_tenant"
                        required="true"
                        showSecondaryValues= "true"
                        selectionChange=".onTenantChange"
                        selectedKey="{view>/tenant_id}"
                        items="{
                            path : '/Org_Tenant'
                        }"
                    >
                        <core:ListItem 
                            key="{tenant_id}" 
                            text="{tenant_name}"
                            additionalText="{tenant_id}"/>
                    </ComboBox>
                    <Title text="{I18N>/COMPANY}" level="H2"/>
                    <ComboBox   
                                id="search_company"
                                required="true"
                                showSecondaryValues= "true"
                                />
                    <SearchField id="midTableSearchField" width="10rem" search=".onPageSearchButtonPress" />
                    <Button id="midTableApplyFilterButton" text="조회" type="Transparent" press=".onPageSearchButtonPress" />
                    <ToolbarSpacer/>
                    <Button id="mainTableAddButton" text="{I18N>/ROW_ADD}" type="Transparent" press=".onMainTableAddButtonPress" enabled="{= ${view>/add} === 'Plant' }"/>
                    <Button id="mainTableDeleteButton" text="{I18N>/ROW_DELETE}" type="Transparent" press=".onMainTableDeleteButtonPress" />
                    <Button id="mainTableSaveButton" text="{I18N>/SAVE}" type="Emphasized" press=".onMainTableSaveButtonPress" />
                </content>
            </OverflowToolbar>
        </headerToolbar>
        <columns>
            <Column id="mainColumnState" minScreenWidth="Tablet" hAlign="Center" width="40px" >
                <ObjectStatus icon="sap-icon://document" text="State" visible="false" />
            </Column>
            <Column id="tenant_id">
                <Text text="tenant_id"/>
            </Column>
            <Column id="company_code">
                <Text text="company_code"/>
            </Column>
            <Column id="plant_code">
                <Text text="plant_code"/>
            </Column>
            <Column id="plant_name">
                <Text text="plant_name"/>
            </Column>
            <Column id="use_flag" hAlign="Center" width="10%">
                <Text text="use_flag"/>
            </Column>
            <Column id="purchase_org_code">
                <Text text="purchase_org_code"/>
            </Column>
            <Column id="bizdivision_code">
                <Text text="bizdivision_code"/>
            </Column>
            <Column id="au_code">
                <Text text="au_code"/>
            </Column>
            <Column id="hq_au_code">
                <Text text="hq_au_code"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem>
                <cells>
                    <ObjectStatus icon="{
                                    path: 'list>_row_state_',
                                    formatter: '.formatter.toModelStateColumnIcon'
                                }" />
                </cells>
                <cells>
                    <Text text="{list>tenant_id}"/>
                </cells>
                <cells>
                    <Text text="{list>company_code}"/>
                </cells>
                <cells>
                    <Input value="{list>plant_code}" editable="{= ${list>_row_state_} === 'C' }" required="true"/>
                </cells>
                <cells>
                    <Input value="{list>plant_name}" required="true"/>
                </cells>
                <cells>
                    <Switch state="{list>use_flag}" customTextOn="Yes" customTextOff="No">
                        <layoutData>
                            <FlexItemData growFactor="1" />
                        </layoutData>
                    </Switch>
                </cells>
                <cells>
                    <Input value="{list>purchase_org_code}"/>
                </cells>
                <cells>
                    <Input value="{list>bizdivision_code}"/>
                </cells>
                <cells>
                    <Input value="{list>au_code}"/>
                </cells>
                <cells>
                    <Input value="{list>hq_au_code}"/>
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</mvc:View>