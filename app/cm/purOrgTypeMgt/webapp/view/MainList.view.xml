<mvc:View controllerName="cm.purOrgTypeMgt.controller.MainList" 
    xmlns:mvc="sap.ui.core.mvc" 
    xmlns="sap.m" 
    xmlns:f="sap.f" 
    xmlns:form="sap.ui.layout.form" 
    xmlns:t="sap.ui.table"
    xmlns:layout="sap.ui.layout" 
    xmlns:core="sap.ui.core" 
    xmlns:lib-m="ext.lib.m"
    height="100%">

    <f:DynamicPage id="page"
        headerExpanded="false" 
        showFooter="{= ${message>/}.length > 0 }"
        toggleHeaderOnTitleClick="true">
        <!-- DynamicPage Title -->
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <!-- <Title text="{I18N>/MESSAGE_MANAGEMENT}" /> -->
                    <Title text="업무유형별 조직관리" />
                </f:heading>
                <f:snappedContent>
                    <form:SimpleForm id="pageSearchFormS" maxContainerCols="2" editable="true" layout="ResponsiveGridLayout" adjustLabelSpan="false" labelSpanL="4" labelSpanM="4" emptySpanL="0" emptySpanM="0" columnsL="2" columnsM="2">
                        <form:content>
                            <VBox>
                                <Label required="true" text="{I18N>/TENANT}: " labelFor="searchChainS" ></Label>
                                <ComboBox   
                                    selectionChange=".onTenantChange"
                                    required="true"
                                    id="search_tenantS"
                                    showSecondaryValues= "true"
                                    width="100%"
                                    items="{path : 'org>/Org_Tenant'}"
                                    >
                                    <core:ListItem 
                                        key="{org>tenant_id}" 
                                        text="{org>tenant_name}"
                                        additionalText="{org>tenant_id}"/>
                                </ComboBox>
                                <layoutData>
                                    <layout:GridData span="XL2 L3 M6 S12" />
                                </layoutData>
                            </VBox>
                            <VBox>
                                <Label required="true" text="{I18N>/COMPANY}: "/>
                                <ComboBox   
                                    id="search_companyS"
                                    required="true"
                                    width="100%"
                                    showSecondaryValues= "true"
                                />
                                <layoutData>
                                    <layout:GridData span="XL2 L3 M6 S12" />
                                </layoutData>
                            </VBox>
                        </form:content>
                    </form:SimpleForm>
                </f:snappedContent>
                <f:expandedContent>
                    <form:SimpleForm id="pageSearchFormE" maxContainerCols="2" editable="true" layout="ResponsiveGridLayout" adjustLabelSpan="false" labelSpanL="4" labelSpanM="4" emptySpanL="0" emptySpanM="0" columnsL="2" columnsM="2">
                        <form:content>
                            <VBox >
                                <Label required="true" text="{I18N>/TENANT}: " ></Label>
                                <ComboBox   
                                    selectionChange=".onTenantChange"
                                    required="true"
                                    id="search_tenantE"
                                    showSecondaryValues= "true"
                                    selectedKey="{view>tenant_id}"
                                    width="100%"
                                    items="{
                                        path : 'org>/Org_Tenant'
                                    }"
                                    >
                                    <core:ListItem 
                                        key="{org>tenant_id}" 
                                        text="{org>tenant_name}"
                                        additionalText="{org>tenant_id}"/>
                                </ComboBox>
                                <layoutData>
                                    <layout:GridData span="XL2 L3 M6 S12" />
                                </layoutData>
                            </VBox>
                            <VBox >
                                <Label required="true" text="{I18N>/COMPANY}: "/>
                                <ComboBox   
                                    id="search_companyE"
                                    required="true"
                                    width="100%"
                                    showSecondaryValues= "true"
                                />
                                <layoutData>
                                    <layout:GridData span="XL2 L3 M6 S12" />
                                </layoutData>
                            </VBox>
                        </form:content>
                    </form:SimpleForm>
                </f:expandedContent>
                <f:actions>
                    <Button id="pageSearchButton" text="{I18N>/SEARCH}" type="Emphasized" press=".onPageSearchButtonPress" />
                </f:actions>
                <f:navigationActions>
                    <Button icon="sap-icon://unfavorite" type="Transparent" />
                </f:navigationActions>
            </f:DynamicPageTitle>
        </f:title>
        <!-- DynamicPage Header -->
        <f:header>
            <f:DynamicPageHeader pinnable="true">
            </f:DynamicPageHeader>
        </f:header>
        <f:content>
        <!-- noDataText="{I18N>/NCM0001}" -->
            <t:Table id="mainTable" 
                    rows="{list>/PurOrgTypeView}"
                    selectionMode="MultiToggle"
                    visibleRowCountMode="Auto"
                    columnResize="onColumnResize"
                    cellClick=".onCellClick"
                    rowSelectionChange=".onCheck"
                    width="auto">
                <!-- <headerToolbar> -->
                <t:extension>
                    <OverflowToolbar>
                        <Title text="{I18N>/MESSAGE} {= ${list>/Message}.length > 0 ? '(' + ${list>/Message}.length + ')' : ''}" level="H2"/>
                        <ToolbarSpacer/>
                        <Button id="mainTableAddButton" text="{I18N>/ROW_ADD}" type="Transparent" press=".onMainTableAddButtonPress" />
                        <Button id="mainTableDeleteButton" text="{I18N>/ROW_DELETE}" type="Transparent" press=".onMainTableDeleteButtonPress" />
                        <Button id="mainTableSaveButton" text="{I18N>/SAVE}" type="Emphasized" press=".onMainTableSaveButtonPress" />
                        <ToolbarSeparator/>
                        <Button icon="sap-icon://action-settings" press=".onMainTablePersoButtonPressed" >
                            <layoutData>
                                <OverflowToolbarLayoutData priority="NeverOverflow" />
                            </layoutData>
                        </Button>
                    </OverflowToolbar>
                <!-- </headerToolbar> -->
                </t:extension>
                <t:columns>
                    <t:Column id="mainColumnState"  hAlign="Center" width="40px" >
                        <ObjectStatus icon="sap-icon://document" text="State" visible="false" />
                        <t:template>
							<ObjectStatus icon="{
                                path: 'list>_row_state_',
                                formatter: '.formatter.toModelStateColumnIcon'
                            }" />
						</t:template>
                    </t:Column>
                    <t:Column id="mainColumntenant_id"  hAlign="Center" width="10em" >
                        <Text text="{I18N>/TENANT_ID}" />
                        <t:template>
							<Input value="{
                                    path: 'list>tenant_id',
                                    type: 'sap.ui.model.type.String',
                                    constraints: {
                                        maxLength: 30
                                    }
                                }" editable="{= ${list>_row_state_} === 'C' }" required="true"></Input>
						</t:template>
                    </t:Column>
                    <t:Column id="mainColumncompany_code" hAlign="Center" width="10em" >
                        <Text text="{I18N>/COMPANY_CODE}" />
                        <t:template>
							<Input value="{
                                    path: 'list>company_code',
                                    type: 'sap.ui.model.type.String',
                                    constraints: {
                                        maxLength: 30
                                    }
                                }" editable="{= ${list>_row_state_} === 'C' }" required="true"></Input>
						</t:template>
                    </t:Column>
                    <t:Column id="mainColumnprocess_type_name" hAlign="Center" width="15em" >
                        <Text text="process_type_name" />
                        <t:template>
							<Input value="{
                                    path: 'list>process_type_name',
                                    type: 'sap.ui.model.type.String',
                                    constraints: {
                                        maxLength: 30
                                    }
                                }" editable="{= ${list>_row_state_} === 'C' }" required="true"></Input>
						</t:template>
                    </t:Column>
                    <t:Column id="mainColumnorg_type_name"  hAlign="Center" width="15em">
                        <Text text="org_type_name" />
                        <t:template>
							<Input value="{list>org_type_name}" required="true" ></Input>
						</t:template>
                    </t:Column>
                    <t:Column id="mainColumnuse_flag"  hAlign="Center" width="10em" >
                        <Text text="{I18N>/USE_FLAG}" />
                        <t:template>
							 <Switch state="{list>use_flag}" customTextOn="Yes" customTextOff="No">
                                <layoutData>
                                    <FlexItemData growFactor="1" />
                                </layoutData>
                            </Switch>
						</t:template>
                    </t:Column>
                </t:columns>
            </t:Table>
        </f:content>
        <!-- DynamicPage Footer -->
		<f:footer>
			<OverflowToolbar>
                <Button
                    icon="sap-icon://alert"
                    type="Reject"
                    text="{= ${message>/}.length }"
                    visible="{= ${message>/}.length > 0 }"
                    ariaHasPopup="Dialog"
                    press=".onMessagePopoverPress" />
				<ToolbarSpacer />
			</OverflowToolbar>
		</f:footer>
    </f:DynamicPage>
</mvc:View>