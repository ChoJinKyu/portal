<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core" 
    xmlns:f="sap.f"
    xmlns:form="sap.ui.layout.form"
    xmlns:layout="sap.ui.layout" >
    <Dialog id="dialogRmk" class="sapUiMediumMargin" title="{I18N>/REMARK}" contentWidth="35%" >
        <VBox class="sapUiMediumMargin">
            <form:SimpleForm id="RmkForm" editable="false" >
                <!-- <form:toolbar>
                    <Toolbar>
                        <Title text="비고"/>
                    </Toolbar>
                </form:toolbar>              -->
                <Label text="{I18N>/REMARK}"/>
                <TextArea id="saveRemark" value="{
                    type: 'sap.ui.model.type.String',
                    constraints: {
                        maxLength: 1500
                    }
                }" required="true" rows="4" />                
            </form:SimpleForm> 
        </VBox>       
      
        <ToolbarSpacer/>
        <HBox alignItems="Center" justifyContent="Center" >
            <Button text="{I18N>/CANCEL}" class="sapUiSmallMargin" press=".onExitRmk"/>
            <Button text="{I18N>/SAVE}" class="sapUiSmallMargin" type="Emphasized" press=".onSaveRmk"/>
        </HBox>
    </Dialog>
</core:FragmentDefinition>
