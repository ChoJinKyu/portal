<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core" 
    xmlns:f="sap.f"
    xmlns:form="sap.ui.layout.form"
    xmlns:layout="sap.ui.layout" >
    <Dialog id="dialogVos" class="sapUiMediumMargin" title="{I18N>/VOS}" contentWidth="35%" >
        <VBox class="sapUiMediumMargin">
            <form:SimpleForm id="VosForm" editable="false" >
                <!-- <Toolbar>
                    <Title text="VOS"/>
                </Toolbar>             -->
                <Label text="{I18N>/VOS}"/>
                <TextArea id="saveSupplierOpinion" value="{
                    type: 'sap.ui.model.type.String',
                    constraints: {
                        maxLength: 1500
                    }
                }" required="true" rows="4" />
            </form:SimpleForm> 
        </VBox>       
      
        <ToolbarSpacer/>
        <HBox alignItems="Center" justifyContent="Center" >
            <Button text="{I18N>/CANCEL}" class="sapUiSmallMargin" press=".onExitVos"/>
            <Button text="{I18N>/SAVE}" class="sapUiSmallMargin" type="Emphasized" press=".onSaveVos"/>
        </HBox>
    </Dialog>
</core:FragmentDefinition>
