_schema-version: "3.1"
ID: sppCap_portal
description: Generated by cds-services-archetype
version: 1.0.0
modules:
- name: sppCap_portal_srv
  type: java
  path: srv
  properties:
    SPRING_PROFILES_ACTIVE: cloud
  requires:
  - name: uaa_sppCap_portal
#  - name: sppDb_hdi_portal
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    disk-quota: 1024M
    memory: 1024M
  build-parameters:
    build-result: target/*.[wj]ar
    builder: custom
    commands:
    - mvn clean package
#- name: sppCap_db_portal
#  type: hdb
#  path: db
#  requires:
#  - name: sppDb_hdi_portal
#  parameters:
#    app-name: sppCap_db_portal
- name: sppCap_portal-approuter
  type: approuter.nodejs
  path: sppCap-approuter
  properties:
    SESSION_TIMEOUT: 720
  requires:
  - name: sppCap_portal_html_repo_runtime
  - name: uaa_sppCap_portal
  - name: dest_sppCap_portal
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  parameters:
    disk-quota: 1024M
    memory: 1024M
- name: sppCap_portal_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: sppCap_portal_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - portal-content.zip
      name: portal
      target-path: resources/
- name: portal
  type: html5
  path: app
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
#- name: sppDb_hdi_portal
#  type: com.sap.xs.hdi-container
#  parameters:
#    config:
#      makeUniqueName: false
#    service: hana
#    service-plan: hdi-shared
#  properties:
#    hdi-service-name: ${service-name}
- name: sppCap_portal_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: sppCap_portal_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: uaa_sppCap_portal
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: sppCap_portal-xsuaa-service
    service-plan: application
- name: dest_sppCap_portal
  type: org.cloudfoundry.managed-service
  parameters:
    service: destination
    service-plan: lite
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
